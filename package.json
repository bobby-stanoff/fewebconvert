{
  "name": "fewebconvert",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx vite build --emptyOutDir",
    "startbuild" : "npm run build && python3 -m http.server -d dist/",
    "dev" : "npx vite"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vite": "^7.2.7"
  }
}
